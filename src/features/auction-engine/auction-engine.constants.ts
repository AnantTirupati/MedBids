export const AUCTION_ENGINE_CONSTANTS = {
  MIN_BID_DECREMENT: 1, // Minimum reduction required for a new bid (in rupees/units)
  AUTO_AWARD_DELAY_MS: 0, // Delay before auto-award triggers
  CLOCK_SYNC_INTERVAL_MS: 1000, // Interval to sync countdown clock
  MAX_BID_AMOUNT: 1000000,
};
